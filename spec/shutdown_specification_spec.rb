RSpec.describe ResqueSelfShutdown::Runner do


  it "has a version number" do
    expect(ResqueSelfShutdown::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
